package com.tawaasol.chat

import kotlinx.coroutines.runBlocking

// Small helper for manual testing from JVM if needed
fun main() {
    println("Twasol Chat - joke module")
    // This is not runnable as-is (needs Android runtime). Kept for IDE convenience.
}
